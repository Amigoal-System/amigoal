# -----------------------------------------------------------------------------
# FIREBASE - CLIENT-SIDE CONFIG
# -----------------------------------------------------------------------------
# These keys are public and are used to initialize Firebase on the client-side.
# Find them in your Firebase project settings under "Your apps" > "Web app" > "Config".
NEXT_PUBLIC_FIREBASE_API_KEY="your_public_api_key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your_project_id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your_project_id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your_project_id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your_messaging_sender_id"
NEXT_PUBLIC_FIREBASE_APP_ID="your_web_app_id"
# Optional: VAPID key for Firebase Cloud Messaging (Push Notifications)
# Find this in Project Settings > Cloud Messaging > Web configuration
NEXT_PUBLIC_FIREBASE_VAPID_KEY="your_vapid_key"


# -----------------------------------------------------------------------------
# FIREBASE - SERVER-SIDE (ADMIN) CONFIG
# -----------------------------------------------------------------------------
# This is a SECRET key for server-side access (e.g., in Genkit flows).
# 1. Go to Firebase Console > Project Settings > Service accounts.
# 2. Click "Generate new private key".
# 3. Open the downloaded JSON file.
# 4. CRITICAL: Encode the ENTIRE content of the JSON file as a Base64 string.
#    - macOS/Linux: `base64 -i [your-file].json`
#    - Windows (PowerShell): `[Convert]::ToBase64String([System.IO.File]::ReadAllBytes("[your-file].json"))`
# 5. Paste the single-line Base64 string here.
FIREBASE_SERVICE_ACCOUNT_KEY="your_base64_encoded_service_account_json"


# -----------------------------------------------------------------------------
# GOOGLE AI - GEMINI API KEY
# -----------------------------------------------------------------------------
# This key is used for all Genkit AI features (chatbots, analysis, etc.).
# Get your key from Google AI Studio: https://aistudio.google.com/app/apikey
NEXT_PUBLIC_GEMINI_API_KEY="your_gemini_api_key"


# -----------------------------------------------------------------------------
# GOOGLE reCAPTCHA v3
# -----------------------------------------------------------------------------
# Used to protect forms from spam and abuse.
# Get your keys from the reCAPTCHA Admin Console: https://g.co/recaptcha/v3
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="your_recaptcha_v3_site_key"
RECAPTCHA_SECRET_KEY="your_recaptcha_v3_secret_key"


# -----------------------------------------------------------------------------
# OTHER CONFIGURATIONS
# -----------------------------------------------------------------------------
# The default contact email for system notifications (e.g., new registrations).
NEXT_PUBLIC_CONTACT_EMAIL="info@amigoal.ch"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
